[package]
name = "nimble_parsec_rs_macro"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Proc-macro entrypoint for compile-time parser generation"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
